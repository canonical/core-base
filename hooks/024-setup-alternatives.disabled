#!/bin/sh

set -eux

# emulate alternatives installed by post install scripts
# for packages we have

# alternatives for vim-tiny
ln -s /usr/bin/vim.tiny /usr/bin/editor
ln -s /usr/bin/vim.tiny /usr/bin/ex
ln -s /usr/bin/vim.tiny /usr/bin/vi
ln -s /usr/bin/vim.tiny /usr/bin/rview
ln -s /usr/bin/vim.tiny /usr/bin/view
