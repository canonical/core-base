diff --git a/Makefile b/Makefile
index 769e060..fe6ea22 100644
--- a/Makefile
+++ b/Makefile
@@ -24,6 +24,9 @@ install:
 		cp /etc/apt/trusted.gpg $(DESTDIR)/etc/apt/ || true; \
 		cp -r /etc/apt/trusted.gpg.d $(DESTDIR)/etc/apt/ || true; \
 	fi
+	# Enable updates
+	sed -n 's/noble-security/noble-updates/p' /etc/apt/sources.list >> $(DESTDIR)/etc/apt/sources.list
+
 	# since recently we're also missing some /dev files that might be
 	# useful during build - make sure they're there
 	[ -e $(DESTDIR)/dev/null ] || mknod -m 666 $(DESTDIR)/dev/null c 1 3
diff --git a/hooks/001-extra-packages.chroot b/hooks/001-extra-packages.chroot
index 739d748..833310a 100755
--- a/hooks/001-extra-packages.chroot
+++ b/hooks/001-extra-packages.chroot
@@ -121,7 +121,7 @@ EOF
 
 # install some packages we need
 apt-get update
-apt-get dist-upgrade -y
+apt-get dist-upgrade -y --allow-downgrades
 
 PACKAGES=(
     apparmor
@@ -171,6 +171,9 @@ PACKAGES=(
     wpasupplicant
     locales
     cracklib-runtime
+    libgcrypt20
+    libgnutls30t64
+    openssl-fips-module-3
 )
 
 case "$(dpkg --print-architecture)" in
@@ -187,7 +191,7 @@ case "$(dpkg --print-architecture)" in
         ;;
 esac
 
-apt-get install --no-install-recommends -y "${PACKAGES[@]}"
+apt-get install --no-install-recommends --allow-downgrades -y "${PACKAGES[@]}"
 
 apt-get autoremove -y
 
diff --git a/static/etc/apt/preferences.d/fips.pref b/static/etc/apt/preferences.d/fips.pref
new file mode 100644
index 0000000..ea86204
--- /dev/null
+++ b/static/etc/apt/preferences.d/fips.pref
@@ -0,0 +1,3 @@
+Package: *
+Pin: release o=LP-PPA-fips-cc-stig-fips-under-certification
+Pin-Priority: 1010
